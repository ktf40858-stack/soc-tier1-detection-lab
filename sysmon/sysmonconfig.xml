<!--
  Sysmon configuration - SOC Tier 1 Detection Lab
  Derived from SwiftOnSecurity/sysmon-config, trimmed to the event IDs the detections in
  this repo consume. A lab config, not a production one: it is deliberately smaller and
  noisier than what you would deploy on a fleet.

  Install : Sysmon64.exe -accepteula -i sysmonconfig.xml
  Update  : Sysmon64.exe -c sysmonconfig.xml

  Events kept:
    1  Process creation        -> SOC-001, SOC-003, SOC-005, SOC-006
    3  Network connection      -> context during triage
    11 File create             -> context during triage
    13 Registry value set      -> SOC-004
    22 DNS query               -> context during triage
-->
<Sysmon schemaversion="4.90">
  <HashAlgorithms>SHA256</HashAlgorithms>
  <CheckRevocation>False</CheckRevocation>

  <EventFiltering>

    <!-- EID 1 : process creation. Log everything, exclude only high-volume signed noise. -->
    <RuleGroup name="ProcessCreate" groupRelation="or">
      <ProcessCreate onmatch="exclude">
        <Image condition="is">C:\Windows\System32\SearchIndexer.exe</Image>
        <Image condition="is">C:\Windows\System32\CompatTelRunner.exe</Image>
        <Image condition="is">C:\Windows\System32\wermgr.exe</Image>
        <Image condition="begin with">C:\Program Files\Windows Defender\</Image>
        <CommandLine condition="contains">\Windows\System32\wevtutil.exe query-events</CommandLine>
      </ProcessCreate>
    </RuleGroup>

    <!-- EID 3 : network connections. Include only what an analyst pivots on. -->
    <RuleGroup name="NetworkConnect" groupRelation="or">
      <NetworkConnect onmatch="include">
        <Image condition="end with">powershell.exe</Image>
        <Image condition="end with">pwsh.exe</Image>
        <Image condition="end with">cmd.exe</Image>
        <Image condition="end with">certutil.exe</Image>
        <Image condition="end with">bitsadmin.exe</Image>
        <Image condition="end with">rundll32.exe</Image>
        <Image condition="end with">mshta.exe</Image>
        <Image condition="end with">regsvr32.exe</Image>
        <Image condition="end with">wscript.exe</Image>
        <Image condition="end with">cscript.exe</Image>
        <DestinationPort condition="is">4444</DestinationPort>
      </NetworkConnect>
    </RuleGroup>

    <!-- EID 11 : file creation in user-writable staging directories -->
    <RuleGroup name="FileCreate" groupRelation="or">
      <FileCreate onmatch="include">
        <TargetFilename condition="contains">\AppData\Local\Temp\</TargetFilename>
        <TargetFilename condition="contains">\Users\Public\</TargetFilename>
        <TargetFilename condition="contains">\Downloads\</TargetFilename>
        <TargetFilename condition="end with">.ps1</TargetFilename>
        <TargetFilename condition="end with">.hta</TargetFilename>
        <TargetFilename condition="end with">.scr</TargetFilename>
      </FileCreate>
    </RuleGroup>

    <!-- EID 13 : registry value set. Autorun locations only. -->
    <RuleGroup name="RegistryEvent" groupRelation="or">
      <RegistryEvent onmatch="include">
        <TargetObject condition="contains">\CurrentVersion\Run</TargetObject>
        <TargetObject condition="contains">\CurrentVersion\RunOnce</TargetObject>
        <TargetObject condition="contains">\CurrentVersion\Policies\Explorer\Run</TargetObject>
        <TargetObject condition="contains">\CurrentVersion\Winlogon\Shell</TargetObject>
        <TargetObject condition="contains">\CurrentVersion\Winlogon\Userinit</TargetObject>
        <TargetObject condition="contains">\Services\</TargetObject>
      </RegistryEvent>
    </RuleGroup>

    <!-- EID 22 : DNS. Exclude the obvious Microsoft telemetry, keep the rest. -->
    <RuleGroup name="DnsQuery" groupRelation="or">
      <DnsQuery onmatch="exclude">
        <QueryName condition="end with">.microsoft.com</QueryName>
        <QueryName condition="end with">.windowsupdate.com</QueryName>
        <QueryName condition="end with">.msftncsi.com</QueryName>
        <QueryName condition="end with">.msedge.net</QueryName>
      </DnsQuery>
    </RuleGroup>

  </EventFiltering>
</Sysmon>
